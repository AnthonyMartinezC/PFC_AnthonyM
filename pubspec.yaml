name: proyecto_final_qr_scanner
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # Iconos
  cupertino_icons: ^1.0.8

  # Firebase
  firebase_core: ^3.14.0
  firebase_auth: ^5.6.0
  cloud_firestore: ^5.6.9

  # QR
  qr_flutter: ^4.1.0
  mobile_scanner: ^7.0.1

  # Utilidades
  permission_handler: ^12.0.0+1
  path_provider: ^2.0.0
  uuid: ^4.0.0

  # Gestión de estado
  provider: ^6.1.2

dev_dependencies:
  screenshot: ^1.3.0
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/